// Record (immutable objects)
const xy = #{ x: 1, y: 2 }; // frozen value type

// Immutable Vector (immutable array)
const xy = #[ x, y ]; // frozen value type

// Immutable Map
const a = ImmutableMap([['x', 1], ['y', 2]]); // frozen value type